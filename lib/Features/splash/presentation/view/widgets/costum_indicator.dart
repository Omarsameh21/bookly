import 'package:flutter/material.dart';

class CostumIndicator extends StatelessWidget {
  const CostumIndicator({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
